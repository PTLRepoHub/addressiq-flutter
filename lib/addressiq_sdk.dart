/// Canonical entry point for the `addressiq_sdk` package.
///
/// ```dart
/// import 'package:addressiq_sdk/addressiq_sdk.dart';
/// ```
///
/// `package:addressiq_sdk/addressiq.dart` remains supported and exports the
/// same surface; pub.dev warns when a package's only library file does not
/// share its name, so this is the conventional entry point.
library addressiq_sdk;

export 'addressiq.dart';
